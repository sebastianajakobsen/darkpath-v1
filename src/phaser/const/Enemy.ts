export const UPDATE_FREQUENCY_CLOSE = 60; // Update every 1 second when close to the player
export const UPDATE_FREQUENCY_FAR = 180; // Update every 3 seconds when far from the player
export const CLOSE_DISTANCE_THRESHOLD = 400000; // Threshold for close distance (e.g., squared distance)